import React from "react";
import Nibble from "./Nibble";

const Byte = ({value}) => {
  return (
    <nobr>
      {value}
      {/* <Nibble />
      <Nibble /> */}
    </nobr>
  );
};

export default Byte;
