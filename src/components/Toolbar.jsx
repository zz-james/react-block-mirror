import React from "react";

import { BlockMirrorTextToBlocks } from "../utils/textToBlocks";
import { BlockMirrorBlockEditor } from "../utils/blockEditor";

const Toolbar = () => {
  return (
    <>
      <p>toolbar</p>
    </>
  );
};

export default Toolbar;
